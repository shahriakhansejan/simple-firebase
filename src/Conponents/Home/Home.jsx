import React from 'react';
import PropTypes from 'prop-types';

const Home = () => {
    return (
        <div>
            <h1>This is Home</h1>
        </div>
    );
};

Home.propTypes = {
    
};

export default Home;